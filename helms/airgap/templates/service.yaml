apiVersion: v1
kind: Service
metadata:
  name: {{ include "airgap.fullname" . }}
  labels:
    {{- include "airgap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airgap.selectorLabels" . | nindent 4 }}
